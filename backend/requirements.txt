# SmartPen Backend Dependencies
# ⚠️ CRITICAL: TensorFlow version locked for InkSight compatibility
# DO NOT change TensorFlow version without verifying InkSight compatibility

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Async HTTP
httpx>=0.25.0

# AI/ML - CRITICAL VERSION CONSTRAINTS
# ⚠️ TensorFlow: Strictly 2.15.0 for InkSight compatibility
# TF 2.15 includes Keras 2.15 natively, avoiding Keras 3 conflicts
tensorflow==2.15.0
transformers>=4.30.0
torch>=2.0.0

# Computer Vision
opencv-python>=4.8.0
# ⚠️ NumPy locked to 1.x for TensorFlow compatibility (TensorFlow 2.16 requires numpy<2)
numpy>=1.24.0,<2.0.0

# OCR
# Note: PaddlePaddle and TensorFlow may have CUDA conflicts
# Consider running in separate environments if issues arise
paddlepaddle>=2.5.0
paddleocr>=2.7.0

# DTW Algorithm
# Use dtw-python library (pollen-robotics), NOT custom implementation
dtw-python>=1.0.0

# Utilities
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
